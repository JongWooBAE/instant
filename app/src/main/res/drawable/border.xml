<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- 테두리 내부 색상 (배경색) -->
    <stroke
        android:color="#FF0000"
    android:width="4dp" />
    <corners android:radius="8dp" />
</shape>
